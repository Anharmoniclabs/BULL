{
  "name": "bull-cloudflare-audit-anchor",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute bull-audit-db --remote --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
